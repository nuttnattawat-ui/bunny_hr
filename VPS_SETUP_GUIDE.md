# 🚀 คู่มือติดตั้ง Bunny HR บน VPS

## ข้อมูล VPS
- **IP Address:** `103.76.182.195`
- **OS:** Ubuntu 22.04/24.04 LTS
- **เข้าถึงได้ที่:** http://103.76.182.195

---

## 📋 สิ่งที่ต้องเตรียม

1. ✅ **IP Address:** 103.76.182.195 (มีแล้ว)
2. ⏳ **Root Password** จาก CloudHosting.co.th
3. ⏳ **SSH Client:**
   - Windows: ใช้ PowerShell (มีอยู่แล้ว)
   - หรือ PuTTY: https://putty.org

---

## 🔧 ขั้นตอนติดตั้ง (ง่ายมาก!)

### **ขั้นตอนที่ 1: เชื่อมต่อ VPS ผ่าน SSH**

เปิด PowerShell แล้วพิมพ์:

```powershell
ssh root@103.76.182.195
```

- พิมพ์ `yes` เมื่อถาม fingerprint
- ใส่ root password ที่ได้จาก CloudHosting

### **ขั้นตอนที่ 2: ดาวน์โหลด Script**

```bash
wget https://raw.githubusercontent.com/nuttnattawat-ui/bunny_hr/main/vps-setup.sh
chmod +x vps-setup.sh
```

### **ขั้นตอนที่ 3: รัน Script (คำสั่งเดียวจบ!)**

```bash
./vps-setup.sh
```

**จะถามข้อมูล 2 อย่าง:**
1. **MySQL Root Password** - ตั้งรหัสผ่านใหม่สำหรับ MySQL (จำไว้)
2. **Database Password** - รหัสผ่านสำหรับ HR System (จำไว้)

### **ขั้นตอนที่ 4: รอติดตั้ง**

⏱️ ใช้เวลา **5-10 นาที** ระบบจะติดตั้งอัตโนมัติ:
- ✅ Node.js 20
- ✅ MySQL Database
- ✅ Nginx Web Server
- ✅ PM2 Process Manager
- ✅ Code จาก GitHub
- ✅ Configuration ทั้งหมด

### **ขั้นตอนที่ 5: เข้าใช้งาน! 🎉**

เปิดเบราว์เซอร์:
```
http://103.76.182.195
```

**Login:**
- Username: `admin`
- Password: `admin123`

---

## 🔄 วิธีอัปเดทระบบ (หลังแก้ code)

### **Local (คอมพิวเตอร์)**
```powershell
git add .
git commit -m "แก้ไข feature XYZ"
git push
```

### **VPS (เซิร์ฟเวอร์)**
```bash
ssh root@103.76.182.195
/root/update-hr.sh
```

✅ **เท่านี้ก็อัปเดทเสร็จ!** (ใช้เวลา 1-2 นาที)

---

## 📝 คำสั่งที่ใช้บ่อย

```bash
# SSH เข้า VPS
ssh root@103.76.182.195

# ดูสถานะระบบ
pm2 status

# ดู log
pm2 logs bunny_hr

# Restart server
pm2 restart bunny_hr

# อัปเดทจาก GitHub
/root/update-hr.sh

# ดู log Nginx
tail -f /var/log/nginx/error.log
```

---

## 🔒 ความปลอดภัย

**เปลี่ยน Password Admin หลังติดตั้ง:**
1. Login เข้าระบบ
2. ไป Settings → Users
3. เปลี่ยนรหัสผ่าน admin

---

## 🐛 แก้ปัญหา

### **ปัญหา: เข้าเว็บไม่ได้**
```bash
# ตรวจสอบ server
pm2 status

# ตรวจสอบ nginx
systemctl status nginx

# Restart ทั้งหมด
pm2 restart bunny_hr
systemctl restart nginx
```

### **ปัญหา: Database error**
```bash
# ตรวจสอบ MySQL
systemctl status mysql

# Login MySQL
mysql -u root -p
```

### **ปัญหา: Port ถูกปิด**
```bash
# เปิด port 80
ufw allow 80/tcp
ufw reload
```

---

## 📞 หากมีปัญหา

1. ดู log: `pm2 logs bunny_hr`
2. ตรวจสอบสถานะ: `pm2 status`
3. Restart: `pm2 restart bunny_hr`
4. บอกผม error message จะช่วยแก้ให้

---

## 🎯 Next Steps (ทำทีหลังได้)

### **1. ติดตั้ง SSL (HTTPS) ฟรี**
```bash
apt install certbot python3-certbot-nginx
# ต้องมี domain ก่อน
```

### **2. ซื้อ Domain**
- Cloudflare: ~300฿/ปี
- ชี้ DNS A Record → 103.76.182.195

### **3. Setup Backup อัตโนมัติ**
```bash
# Backup ทุกวันเวลา 2:00
crontab -e
0 2 * * * mysqldump -u root -p'password' hr_system > /backup/hr_$(date +\%Y\%m\%d).sql
```

---

## ✅ Checklist

- [ ] SSH เข้า VPS ได้
- [ ] รัน vps-setup.sh เสร็จ
- [ ] เข้า http://103.76.182.195 ได้
- [ ] Login admin/admin123 ได้
- [ ] ทดสอบสร้างพนักงาน
- [ ] ทดสอบลงเวลา
- [ ] เปลี่ยน password admin

---

**พร้อมใช้งานแล้ว! 🚀**
